import 'package:flutter/material.dart';

class ScheduleScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('도담도담'),
        actions: [
          IconButton(
            icon: Icon(Icons.share),
            onPressed: () {
              // 카카오톡 공유 기능 구현 예정
            },
          ),
        ],
      ),
      body: Column(
        children: [
          // 상단 프로필 섹션
          Padding(
            padding: const EdgeInsets.all(16.0),
            child: Row(
              children: [
                CircleAvatar(
                  radius: 30,
                  backgroundColor: Colors.grey[300],
                  child: Icon(
                    Icons.person,
                    size: 30,
                    color: Colors.grey[700],
                  ),
                ),
                SizedBox(width: 16),
                Column(
                  crossAxisAlignment: CrossAxisAlignment.start,
                  children: [
                    Text(
                      '"김도담" 14개월 남',
                      style: TextStyle(
                        fontSize: 18,
                        fontWeight: FontWeight.bold,
                      ),
                    ),
                    SizedBox(height: 4),
                    Text(
                      '1월 17일 (금)',
                      style: TextStyle(fontSize: 16, color: Colors.grey[600]),
                    ),
                  ],
                ),
                Spacer(),
                IconButton(
                  icon: Icon(Icons.more_vert),
                  onPressed: () {
                    // 추가 옵션
                  },
                ),
              ],
            ),
          ),
          Divider(thickness: 1),
          // 상단 아이콘 섹션
          Padding(
            padding: const EdgeInsets.symmetric(vertical: 16.0),
            child: Row(
              mainAxisAlignment: MainAxisAlignment.spaceEvenly,
              children: [
                Column(
                  children: [
                    Icon(Icons.restaurant, color: Colors.red, size: 40),
                    SizedBox(height: 8),
                    Text('식사', style: TextStyle(fontSize: 14)),
                  ],
                ),
                Column(
                  children: [
                    Icon(Icons.accessibility_new, color: Colors.blue, size: 40),
                    SizedBox(height: 8),
                    Text('기저귀', style: TextStyle(fontSize: 14)),
                  ],
                ),
                Column(
                  children: [
                    Icon(Icons.bedtime, color: Colors.purple, size: 40),
                    SizedBox(height: 8),
                    Text('취침', style: TextStyle(fontSize: 14)),
                  ],
                ),
                Column(
                  children: [
                    Icon(Icons.cloud, color: Colors.orange, size: 40),
                    SizedBox(height: 8),
                    Text('기상', style: TextStyle(fontSize: 14)),
                  ],
                ),
              ],
            ),
          ),
          Divider(thickness: 1),
          // 타임라인 섹션
          Expanded(
            child: ListView(
              children: [
                ListTile(
                  leading: Icon(Icons.access_time, color: Colors.yellow),
                  title: Text('12:00'),
                  subtitle: Text('식사'),
                ),
                // 추가 일정 항목 구현
              ],
            ),
          ),
          // 하단 버튼 섹션
          Padding(
            padding:
                const EdgeInsets.symmetric(vertical: 16.0, horizontal: 16.0),
            child: Row(
              mainAxisAlignment: MainAxisAlignment.spaceBetween,
              children: [
                ElevatedButton(
                  onPressed: () {
                    // 사진 첨부 기능 구현 예정
                  },
                  child: Text('사진 첨부'),
                ),
                ElevatedButton(
                  onPressed: () {
                    // 메모 추가 기능 구현 예정
                  },
                  child: Text('메모 추가'),
                ),
              ],
            ),
          ),
        ],
      ),
      bottomNavigationBar: BottomNavigationBar(
        currentIndex: 1, // "아이 일정 관리" 페이지
        onTap: (index) {
          // 탭 변경 시 페이지 이동
        },
        items: [
          BottomNavigationBarItem(
            icon: Icon(Icons.chat),
            label: 'AI로 놀이 추천받기',
          ),
          BottomNavigationBarItem(
            icon: Icon(Icons.calendar_today),
            label: '아이 일정 관리',
          ),
          BottomNavigationBarItem(
            icon: Icon(Icons.local_hospital),
            label: '근처 소아과 찾기',
          ),
        ],
      ),
    );
  }
}
